#ifndef TYPES_HPP
#define TYPES_HPP

// struct func_base;
// struct metric_base;
// struct dofs_base;

template<class F> struct func;
template<class G> struct dofs;
template<class G> struct metric;




#endif
